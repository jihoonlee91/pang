## Summary

What does this PR change and why?

## Related docs

Which `docs/design/phaseX_Y.md` (or `docs/PRD.md` / `docs/PLAN.md`) did you update, if any? This repo follows a docs-first workflow — design docs should be updated before or alongside the implementation.

## Checklist

- [ ] `npx tsc --noEmit -p tsconfig.app.json` passes
- [ ] `npm run build` passes
- [ ] `npm run lint` passes
- [ ] Manually tested in the browser (`npm run dev`)
